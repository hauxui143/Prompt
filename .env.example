# AI Prompt Enhancer Environment Variables

# Note: This application doesn't require server-side environment variables
# Users provide their OpenAI API key directly in the UI for security
# The API key is only used client-side and never stored

# Optional: You can set a default API key for development (NOT recommended for production)
# OPENAI_API_KEY=your_openai_api_key_here

# Next.js Configuration
NEXT_PUBLIC_APP_NAME="AI Prompt Enhancer"
NEXT_PUBLIC_APP_DESCRIPTION="Transform your prompts with AI"

# For production deployment, make sure to:
# 1. Never commit actual API keys to version control
# 2. Use secure environment variable management
# 3. Users should always provide their own API keys