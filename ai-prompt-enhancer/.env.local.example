# AI Prompt Enhancer - Local Environment Variables

# This application doesn't require server-side environment variables
# Users provide their OpenAI API key directly in the UI with session management

# Optional: App configuration
NEXT_PUBLIC_APP_NAME="AI Prompt Enhancer"
NEXT_PUBLIC_APP_DESCRIPTION="Transform your prompts with AI"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# For development only (NOT for production)
# You can set a test API key here, but users should always provide their own
# OPENAI_API_KEY=your_development_key_here

# Next.js configuration
NEXT_TELEMETRY_DISABLED=1

# Notes:
# 1. Copy this file to .env.local for local development
# 2. Never commit actual API keys to version control
# 3. Users provide their own API keys in the application UI
# 4. API keys are stored with session management (24-hour expiry)